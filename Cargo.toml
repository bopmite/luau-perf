[package]
name = "luperf"
version = "0.1.0"
edition = "2021"

[dependencies]
full_moon = { version = "2", features = ["luau"] }
walkdir = "2"
rayon = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[profile.release]
lto = true
codegen-units = 1
strip = true
